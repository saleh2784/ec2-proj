pipeline {
    agent any
    
    parameters {
        choice (name: 'process', choices: ['install', 'uninstall'], description: 'Choose to install or uninstall')
    }

    stages {
        stage('Clean') {
            steps {
                cleanWs()  // Clean the workspace
            }
        }
        stage('Check PATH') {
            steps {
                sh 'echo $PATH'
                sh 'which ansible-playbook'
            }
        }

        stage('Install') {
            when {
                expression { params.process == 'install' }
            }
            steps {
                ansiColor('xterm') {  // Wrap steps that need colored output
                    ansiblePlaybook(
                        playbook: "install.yml",
                        inventory: "hosts.ini",
                        become: true,
                        extras: '-vvv',
                        forks: 1,
                        disableHostKeyChecking: true,
                        credentialsId: 'ssh-key',
                        colorized: true
                    )
                }
            }
        }

        stage('Uninstall') {
            when {
                expression { params.process == 'uninstall' }
            }
            steps {
                ansiColor('xterm') {  // Wrap steps that need colored output
                    ansiblePlaybook(
                        playbook: "uninstall.yml",
                        inventory: "hosts.ini",
                        become: true,
                        extras: '-vvv',
                        forks: 1,
                        disableHostKeyChecking: true,
                        credentialsId: 'ssh-key',
                        colorized: true
                    )
                }
            }
        }
    }
}
